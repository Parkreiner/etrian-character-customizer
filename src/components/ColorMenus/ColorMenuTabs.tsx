import { ColorCategory } from "@/typesConstants/colors";

type Props = {
  setActiveCategory: (category: ColorCategory) => void;
};

export default function ColorMenuTabs({ setActiveCategory }: Props) {
  return null;
}
